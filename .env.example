GEMINI_API_KEY=
KRAVA_APP_KEY=

# Linq: leave LINQ_MODE unset or "demo" for hackathon (no API keys required).
# Set LINQ_MODE=live plus keys below when you have partner access.
LINQ_MODE=demo
LINQ_API_KEY=
LINQ_FROM_PHONE=
DEMO_APPROVER_PHONE=
LINQ_WEBHOOK_SIGNING_SECRET=

# Vercel: add Upstash Redis from Marketplace → Storage (auto-injects UPSTASH_REDIS_REST_URL / TOKEN).
